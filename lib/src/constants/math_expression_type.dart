/// Defines the output format for a mathematical expression.
enum MathExpressionType {
  /// A string formatted for rendering with a LaTeX engine.
  latex,

  /// A string formatted for computation or analysis (e.g., a Dart-like string).
  dart,
}
